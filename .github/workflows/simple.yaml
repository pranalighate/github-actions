name: simple
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps: 
      - name: echo string
        run: echo "Hello"
      - name: multiple command
        run: |
          npm -v
          node -v